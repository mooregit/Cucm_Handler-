# Project File Structure

Below is the current repository file structure for this project (as of the latest commit).
The tree lists files and notable directories including a short description of important files.

```
Cucm_Handler-/
├── .env                           # (optional) environment variable file (currently empty)
├── .git/                          # Git repository metadata
├── .gitignore
├── README.md                      # Project README (updated)
├── requirements.txt               # Python dependencies (currently blank)
└── app/
    ├── __init__.py
    ├── main.py                    # (skeleton) FastAPI main application entry point
    ├── api/                       # REST routers (FastAPI)
    │   ├── router_axl_raw.py      # Raw AXL introspection/call router
    │   ├── router_devices.py      # (stub) device-related endpoints
    │   ├── router_health.py       # (stub) health-check endpoints
    │   ├── router_perfmon_raw.py  # (stub) raw perfmon endpoints
    │   ├── router_ris_raw.py      # (stub) raw RIS endpoints
    │   ├── router_status.py       # Cluster status aggregation endpoints
    │   └── router_users.py        # (stub) user management endpoints
    ├── axl/                       # AXL SOAP client and helper services
    │   ├── axl_client.py          # Dynamic Zeep-based AXL client wrapper
    │   └── axl_service.py         # Higher-level AXL helpers (users, phones, lines, trunks, cluster summaries)
    ├── ris/                       # RISPort SOAP client and helpers
    │   ├── ris_client.py          # RIS client wrapper for realtime status
    │   └── ris_service.py         # Convenience methods for registered devices/trunks
    ├── perfmon/                   # PerfMon SOAP client and helpers
    │   ├── perfmon_client.py
    │   └── perfmon_service.py
    ├── core/                      # Loader utilities and config helpers
    │   ├── axl_loader.py          # (placeholder) should expose `get_axl_client()`
    │   ├── config.py              # (placeholder) configuration helpers
    │   ├── perfmon_loader.py      # Factory method for PerfMon client from env
    │   └── ris_loader.py          # Factory method for RIS client from env
    ├── models/                    # SQLAlchemy models
    │   ├── base.py                # Base mixins and helpers (UUID primary, timestamps, soft delete)
    │   ├── uc_base.py             # Abstract UC server base model
    │   ├── tenant.py              # Tenant model (multi-tenant grouping)
    │   ├── cucm_server.py         # CUCM server model
    │   ├── expressway_server.py   # Expressway server model
    │   ├── imp_server.py          # IMP server model
    │   ├── cube_gateway.py        # CUBE / PSTN gateway model
    │   ├── ucce_server.py         # UCCE server model
    │   ├── uccx_server.py         # UCCX server model
    │   ├── unity_connection_server.py
    │   ├── cms_server.py
    │   └── cer_server.py
    ├── schemas/                   # Pydantic schema files (mostly placeholder)
    │   ├── __init__.py
    │   ├── axl.py                 # AXL-specific schema placeholders
    │   ├── cucm.py                # CUCM model schemas (empty placeholder)
    │   ├── device.py              # Device schema placeholder
    │   ├── perfmon.py             # PerfMon schema placeholder
    │   ├── risport.py             # RIS port schema placeholder
    │   ├── user.py                # User schema placeholder
    │   └── common.py              # Shared Pydantic types / utilities (empty placeholder)
    └── wsdl/                      # WSDL files for Zeep (empty placeholder)
```

Notes:
- Several router files under `app/api` are currently placeholders and require implementation.
- `app/core/axl_loader.py` and `app/main.py` are present but not fully implemented; `ris_loader.py` and `perfmon_loader.py` show how other loaders work.
- The `app/models` folder contains SQLAlchemy models that use Postgres-specific types (INET, UUID).
- The repo expects WSDL files to be provided by the user or placed under the `app/*/schema` or `app/wsdl` folder (ENV vars let you override paths).

If you'd like, I can also: add a runnable `app/main.py`, implement `axl_loader.py`, populate example WSDLs, or create a `requirements.txt` with pinned versions.

---

Generated by script / assistant — run `tree` locally or inspect the files above for a more exhaustive view.

